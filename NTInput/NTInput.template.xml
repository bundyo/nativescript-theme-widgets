<GridLayout rows="auto, auto" xmlns:ui="nativescript-theme-widgets">
    <!--<GridLayout row="1" columns="*, auto" class="input__validation" id="input__validation" tap="{{ showPopup }}">
        <label col="1" class="nt-input__validation-shadow" />
        <StackLayout class="nt-input__validation-text">
            <Label text="Remember, remember the fifth of November!" />
        </StackLayout>
        <label col="1" class="nt-input__validation-arrow" />
    </GridLayout>-->
    <Label class="nt-input__label" text="{{ label }}" visibility="{{ label !== undefined ? 'visible' : 'collapsed' }}" />
    <ui:NTIcon row="1" class="nt-input__icon" name="{{ valid ? 'check-circle' : 'exclamation-circle' }}" tap="{{ showPopup }}" />
    <TextField row="1" class="nt-input__field" />
<!--    visibility="{{ !valid ? 'visible' : 'collapsed' }}"-->
</GridLayout>
