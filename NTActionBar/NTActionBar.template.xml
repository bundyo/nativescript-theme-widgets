<GridLayout class="nt-action-bar__container" columns="auto, *" xmlns:ui="nativescript-theme-widgets">
    <ui:NTIcon class="nt-action-bar__nav-button" name="{{ back ? 'chevron-left' : menu ? 'bars' : undefined }}"
                  tap="{{ onTap }}" visibility="{{ menu || backButton ? 'visible' : 'collapsed' }}"/>
    <Label col="1" class="nt-action-bar__title" text="{{ title }}" ios:horizontalAlignment="center" />
</GridLayout>
